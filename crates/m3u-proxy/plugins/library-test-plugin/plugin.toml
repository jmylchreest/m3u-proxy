[plugin]
name = "library-test-plugin"
version = "0.1.0"
description = "Tests WASM compatibility of Rust libraries"
author = "M3U-Proxy Team"
license = "MIT"

[capabilities]
stages = [
    "data_mapping",
]